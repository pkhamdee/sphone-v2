apiVersion: v1
kind: Service
metadata:
  name: sphone-postgres
  namespace: sphone
  labels:
    app: sphone-postgres
spec:
  selector:
    app: sphone-postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  type: ClusterIP
  clusterIP: None  # Headless for StatefulSet DNS
